resource <%= name %> {
  device /dev/drbd_<%= name %> minor <%= minor %>;
  disk   <%= disk %>;
  meta-disk internal;
  on <%= drbdMasterName %> {
    address <%= drbdMasterIp %>:<%= port %>;
  }
  on <%= drbdSlaveName %> {
    address <%= drbdSlaveIp %>:<%= port %>;
  }
}
